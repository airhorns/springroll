<% if !item.sizes.blank? %>
	<% if item.sizes.length == 1 %>
		<%= size_button(item.sizes[0]) %>
	<% else %>
		<% for size in item.sizes %>
	    	<%= size_button(size) %>
		<% end %>
	<% end %>
<% else %>
	Currently Unavailable
<% end %>
